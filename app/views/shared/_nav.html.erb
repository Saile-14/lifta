<nav class="site-nav">
  <%= link_to "Lifta", authenticated? ? dashboard_path : root_path, class: "site-nav__brand" %>
  <% if authenticated? %>
    <%= link_to "Dashboard", dashboard_path %>
    <%= link_to "Log a lift", new_lift_path %>
    <% if Current.user.admin? %>
      <% pending_count = Lift.pending.count %>
      <%= link_to admin_root_path do %>Admin<% if pending_count.positive? %><span class="site-nav__count"><%= pending_count %></span><% end %><% end %>
    <% end %>
    <span class="site-nav__spacer"></span>
    <span><%= Current.user.email_address %></span>
    <%= link_to "Sign out", session_path, data: { turbo_method: :delete } %>
  <% else %>
    <span class="site-nav__spacer"></span>
    <%= link_to "Sign in", new_session_path %>
    <%= link_to "Sign up", new_registration_path %>
  <% end %>
</nav>
