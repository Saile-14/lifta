<% page_title t(".title") %>

<div class="box">
  <div class="box__header"><%= t(".title") %></div>
  <div class="box__body">
    <%= render "form", entry: @bodyweight_entry, values: @values %>
    <p class="muted"><%= t(".note") %></p>
    <p><%= link_to t(".back"), bodyweight_entries_path, class: "btn-secondary" %></p>
  </div>
</div>
