<% page_title t(".title") %>

<div class="box">
  <div class="box__header"><%= t(".title") %></div>
  <div class="box__body">
    <%= discipline_tabs(@discipline) { |discipline| new_lift_path(discipline: discipline) } %>
    <%= render "form", lift: @lift, discipline: @discipline, values: @values %>
  </div>
</div>
