# See https://git-scm.com/docs/gitattributes for more about git attribute files.

# Mark the database schema as having been generated.
db/schema.rb linguist-generated

# Mark any vendored files as having been vendored.
vendor/* linguist-vendored
config/credentials/*.yml.enc diff=rails_credentials
config/credentials.yml.enc diff=rails_credentials

# Scripts run on Linux (CI runners, the Docker image, Kamal), where a CR
# would break the shebang or confuse the shell. Windows checkouts get LF
# for these regardless of core.autocrlf; the .cmd shims are the exception,
# since those are read by cmd.exe.
bin/* text eol=lf
bin/*.cmd text eol=crlf
*.sh text eol=lf
